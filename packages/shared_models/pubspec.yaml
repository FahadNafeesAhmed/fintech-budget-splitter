name: shared_models
description: Shared models and business logic for the FinTech Budget Splitter.
version: 1.0.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  meta: ^1.9.1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

name: frontend
description: Coin Catcher — a Flutter web game powered by DartStream.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  dartstream_client: ^0.0.1
  google_fonts: ^8.1.0
  openfeature_dart_server_sdk: ^0.0.22
  openfeature_provider_intellitoggle: ^0.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

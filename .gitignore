# Dart/Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
# pubspec.lock is committed for applications (frontend + backend) for
# reproducible builds; only pure libraries (packages/shared_models) ignore it.
packages/shared_models/pubspec.lock

# Dart Frog
backend/.dart_frog/

# Firebase
.firebase/
**/google-services.json
**/GoogleService-Info.plist

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

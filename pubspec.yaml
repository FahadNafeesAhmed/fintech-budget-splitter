name: fintech_smoke
description: End-to-end smoke + deep-dive CLIs for the FinTech Budget Splitter sample against the DartStream dev API.
publish_to: none

# Min Dart 3.12.0; tracks latest stable. See README Prerequisites.
environment:
  sdk: ^3.12.0

dependencies:
  http: ^1.2.2
  openfeature_dart_server_sdk: ^0.0.22
  openfeature_provider_intellitoggle: ^0.0.9
